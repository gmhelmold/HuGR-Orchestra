{
  "product": "b0c33d2f6567a2c741240f3c44bc00ca2f01e7e7",
  "node": "v22.17.1",
  "transport": "original compiled CLI with original worker threads",
  "model": "offline abstaining command",
  "index": "controlled SCIP, not external indexer output",
  "cases": [
    {
      "arm": "advisory",
      "exit": 0,
      "signal": null,
      "stdout": "genesis: seeded 0 candidate fact(s); ratified 0\ncost: llmCalls 2 · budgetSpent 2\nprompt: 441a39237b4e933bd685c122cb223214bf0a69627ecd98054f08e27e8d34ceb1 — the artifact every proposal on this run was built from\nmine: 0 candidate facts — 2 site(s) visited and every one abstained: nothing was proposed or admitted (facts are never fabricated)\ncoverage: coverage CLOSES — all 2 planned site(s) accounted for: 0 seeded, 2 abstained, 0 unrecorded, 0 interrupted, 0 never visited\nsite: {\"rank\":1,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/util.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\nsite: {\"rank\":2,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/app.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\n",
      "stderr": "",
      "prompts": [
        {
          "sha256": "1e8eade32c4c06090a91bde1032553deb1ac95aa143cc9d9a1e4bece05ae60e0",
          "dependency": false,
          "count": false,
          "block": true,
          "prompt": "You are shown ONE anchored unit from a real codebase, and nothing else.\n\n<unit path=\"src/app.ts\" name=\"src/app.ts\">\nimport { greet } from './util';\nexport function main(): string { return greet('world'); }\n\n</unit>\n\nState ONE fact about this unit that a competent engineer would NOT already know from its name and\nsignature, and that would change what they do. Work in two steps.\n\nSTEP 1 — REASON FREELY (scratch, never stored). Read the bytes closely. Draft a candidate fact, then actively\ntry to REFUTE it against the source:\n- Is it derivable from THESE bytes, or are you leaning on what you know about a library, framework or\n  convention that is not visible here — or on a stale/past-tense COMMENT the current code may contradict?\n- Does the fact's truth rest on a COMMENT'S CLAIM about something NOT in these bytes — another file or\n  module, a task/PR/issue number (like #123), what the code \"used to\" do, or the absence of something\n  elsewhere in the codebase? If so, discard it: even an ACCURATE comment about elsewhere is a fact about\n  THAT code, not this unit, and does not re-derive at THIS unit's bytes. The surviving fact must be\n  checkable by reading the CODE in this unit itself — its declarations, control flow, types, or literals —\n  not by trusting what a comment asserts about code you cannot see here.\n- Does the claim assert an EXACT COUNT or an EXHAUSTIVE enumeration (\"N of the M items\", \"every X except\n  Y\", \"all fourteen …\")? A number or a complete list is admissible ONLY if you actually counted or listed\n  it in the shown bytes and re-checked it item by item — a miscount or a mislabelled item makes an\n  otherwise-true fact FALSE. Prefer the qualitative form (\"only X uses the type-only form, unlike its\n  siblings\") over a numeric one (\"N of the M re-exports …\") unless the exact number is itself the point and\n  you have verified it against the bytes.\n- Is it more than a restatement of the name, the signature or the types, or a summary of what the code\n  plainly does?\nDiscard any candidate that does not survive. This reasoning is scratch: it is parsed away and never persisted.\n\nSTEP 2 — EMIT THE RESULT. If a fact survived, emit it as EXACTLY ONE fenced block tagged `atlas-fact`, holding\na JSON object with a single `claim` field — one sentence of plain prose, derivable from the CODE bytes above\n(checkable by reading this unit itself, not from a comment's claim about code you cannot see here):\n\n```atlas-fact\n{\"claim\": \"<the one surviving fact>\"}\n```\n\nEmit exactly ONE such block, and put NO fenced block anywhere in your reasoning — only the final fact is fenced.\n\nIf no such fact survives, emit NO fact block at all (a bare `NO-FACT` on its own line is also accepted). Most\nunits hold none. That is a correct, expected result — recorded as a deliberate abstention, never a failure. A\nfact the bytes do not contain cannot be re-derived at source@sha and MUST NOT be emitted.\n"
        },
        {
          "sha256": "0514c1445592f8475d4d2c87dd554225d0690eb6491a4901dba9e7ed1178397f",
          "dependency": false,
          "count": false,
          "block": true,
          "prompt": "You are shown ONE anchored unit from a real codebase, and nothing else.\n\n<unit path=\"src/util.ts\" name=\"src/util.ts\">\nexport function greet(n: string): string { return `hi ${n}`; }\n\n</unit>\n\nState ONE fact about this unit that a competent engineer would NOT already know from its name and\nsignature, and that would change what they do. Work in two steps.\n\nSTEP 1 — REASON FREELY (scratch, never stored). Read the bytes closely. Draft a candidate fact, then actively\ntry to REFUTE it against the source:\n- Is it derivable from THESE bytes, or are you leaning on what you know about a library, framework or\n  convention that is not visible here — or on a stale/past-tense COMMENT the current code may contradict?\n- Does the fact's truth rest on a COMMENT'S CLAIM about something NOT in these bytes — another file or\n  module, a task/PR/issue number (like #123), what the code \"used to\" do, or the absence of something\n  elsewhere in the codebase? If so, discard it: even an ACCURATE comment about elsewhere is a fact about\n  THAT code, not this unit, and does not re-derive at THIS unit's bytes. The surviving fact must be\n  checkable by reading the CODE in this unit itself — its declarations, control flow, types, or literals —\n  not by trusting what a comment asserts about code you cannot see here.\n- Does the claim assert an EXACT COUNT or an EXHAUSTIVE enumeration (\"N of the M items\", \"every X except\n  Y\", \"all fourteen …\")? A number or a complete list is admissible ONLY if you actually counted or listed\n  it in the shown bytes and re-checked it item by item — a miscount or a mislabelled item makes an\n  otherwise-true fact FALSE. Prefer the qualitative form (\"only X uses the type-only form, unlike its\n  siblings\") over a numeric one (\"N of the M re-exports …\") unless the exact number is itself the point and\n  you have verified it against the bytes.\n- Is it more than a restatement of the name, the signature or the types, or a summary of what the code\n  plainly does?\nDiscard any candidate that does not survive. This reasoning is scratch: it is parsed away and never persisted.\n\nSTEP 2 — EMIT THE RESULT. If a fact survived, emit it as EXACTLY ONE fenced block tagged `atlas-fact`, holding\na JSON object with a single `claim` field — one sentence of plain prose, derivable from the CODE bytes above\n(checkable by reading this unit itself, not from a comment's claim about code you cannot see here):\n\n```atlas-fact\n{\"claim\": \"<the one surviving fact>\"}\n```\n\nEmit exactly ONE such block, and put NO fenced block anywhere in your reasoning — only the final fact is fenced.\n\nIf no such fact survives, emit NO fact block at all (a bare `NO-FACT` on its own line is also accepted). Most\nunits hold none. That is a correct, expected result — recorded as a deliberate abstention, never a failure. A\nfact the bytes do not contain cannot be re-derived at source@sha and MUST NOT be emitted.\n"
        }
      ]
    },
    {
      "arm": "dependency",
      "exit": 0,
      "signal": null,
      "stdout": "genesis: seeded 0 candidate fact(s); ratified 0\ncost: llmCalls 2 · budgetSpent 2\nprompt: 6015c264c18580c396befc5c2634feda6b1540d074e4ae0e46ad4acf6789ed67 — the artifact every proposal on this run was built from\nmine: 0 candidate facts — 2 site(s) visited and every one abstained: nothing was proposed or admitted (facts are never fabricated)\ncoverage: coverage CLOSES — all 2 planned site(s) accounted for: 0 seeded, 2 abstained, 0 unrecorded, 0 interrupted, 0 never visited\nsite: {\"rank\":1,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/app.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\nsite: {\"rank\":2,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/util.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\n",
      "stderr": "",
      "prompts": [
        {
          "sha256": "bc3bc467d1d98aa194491550b56b45c49d4bfe8c6f0c84bec6a875a098e6c3fc",
          "dependency": true,
          "count": false,
          "block": false,
          "prompt": "You are shown ONE anchored unit from a real codebase, and a CLOSED LIST of the symbols it actually depends on\n(resolved from the code index — every name below is a real cross-unit dependency of this unit).\n\n<unit path=\"src/app.ts\" name=\"src/app.ts\">\nimport { greet } from './util';\nexport function main(): string { return greet('world'); }\n\n</unit>\n\nDEPENDS-ON candidates (the closed list — select ONLY from these): greet\n\nFrom that list, pick the ONE dependency a competent engineer would NOT already expect from this unit's name and\nrole, and whose relationship to this unit is worth recording as knowledge.\n\n- The name you output MUST be copied VERBATIM from the candidate list above. Do not name anything else.\n- Restating an obvious dependency (the framework the unit is plainly built on) is not worth recording. Pick the\n  non-obvious one.\n- If the candidate list is empty, or none of its entries is a non-obvious dependency worth recording, output the\n  single token `NO-FACT` and nothing else. That is a correct, expected abstention, never a failure.\n\nOutput EITHER exactly one line `DEPENDS-ON: <name>` (with `<name>` copied verbatim from the list), OR the single\ntoken `NO-FACT`. No preamble, no reasoning, no confidence, no formatting.\n"
        },
        {
          "sha256": "11089f5c46ca59bf15839a9eec9cdcd270fa922a13941d7b15d374fc5280c68f",
          "dependency": true,
          "count": false,
          "block": false,
          "prompt": "You are shown ONE anchored unit from a real codebase, and a CLOSED LIST of the symbols it actually depends on\n(resolved from the code index — every name below is a real cross-unit dependency of this unit).\n\n<unit path=\"src/util.ts\" name=\"src/util.ts\">\nexport function greet(n: string): string { return `hi ${n}`; }\n\n</unit>\n\nDEPENDS-ON candidates (the closed list — select ONLY from these): \n\nFrom that list, pick the ONE dependency a competent engineer would NOT already expect from this unit's name and\nrole, and whose relationship to this unit is worth recording as knowledge.\n\n- The name you output MUST be copied VERBATIM from the candidate list above. Do not name anything else.\n- Restating an obvious dependency (the framework the unit is plainly built on) is not worth recording. Pick the\n  non-obvious one.\n- If the candidate list is empty, or none of its entries is a non-obvious dependency worth recording, output the\n  single token `NO-FACT` and nothing else. That is a correct, expected abstention, never a failure.\n\nOutput EITHER exactly one line `DEPENDS-ON: <name>` (with `<name>` copied verbatim from the list), OR the single\ntoken `NO-FACT`. No preamble, no reasoning, no confidence, no formatting.\n"
        }
      ]
    },
    {
      "arm": "count",
      "exit": 0,
      "signal": null,
      "stdout": "genesis: seeded 0 candidate fact(s); ratified 0\ncost: llmCalls 2 · budgetSpent 2\nprompt: caa515d1a7c950b252546ee6e1286ea224731cc6688f942fda26db601c7f7bb3 — the artifact every proposal on this run was built from\nmine: 0 candidate facts — 2 site(s) visited and every one abstained: nothing was proposed or admitted (facts are never fabricated)\ncoverage: coverage CLOSES — all 2 planned site(s) accounted for: 0 seeded, 2 abstained, 0 unrecorded, 0 interrupted, 0 never visited\nsite: {\"rank\":1,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/util.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\nsite: {\"rank\":2,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/app.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\n",
      "stderr": "",
      "prompts": [
        {
          "sha256": "6fa47db50ad21fd13aca26f94e5b8974ed0e5529564b7811a10994874eff4fd0",
          "dependency": false,
          "count": true,
          "block": false,
          "prompt": "You are shown ONE anchored unit from a real codebase, and a CLOSED LIST of the symbols THIS unit exports that are\nreferenced by OTHER units (resolved from the code index — every name below is a real, externally-called export of\nthis unit).\n\n<unit path=\"src/util.ts\" name=\"src/util.ts\">\nexport function greet(n: string): string { return `hi ${n}`; }\n\n</unit>\n\nExternally-referenced exports (the closed list — select ONLY from these): greet\n\nFrom that list, pick the ONE export whose broad fan-in a competent engineer would NOT already expect from this\nunit's name and role — the symbol that is quietly a hub, whose \"referenced across the codebase\" status is worth\nrecording as knowledge.\n\n- The name you output MUST be copied VERBATIM from the candidate list above. Do not name anything else.\n- Do NOT state a number. Atlas computes the caller count from the index; your job is only to pick the salient name.\n- Restating an obvious hub (the export the unit plainly exists to provide) is not worth recording. Pick the\n  non-obvious one.\n- If the candidate list is empty, or none of its entries has a non-obvious fan-in worth recording, output the\n  single token `NO-FACT` and nothing else. That is a correct, expected abstention, never a failure.\n\nOutput EITHER exactly one line `COUNT: <name>` (with `<name>` copied verbatim from the list), OR the single token\n`NO-FACT`. No preamble, no reasoning, no number, no confidence, no formatting.\n"
        },
        {
          "sha256": "d54bc12a344f40ea6a43b27c4eb87a535134ece4edec81c2fba4dbb698230a8f",
          "dependency": false,
          "count": true,
          "block": false,
          "prompt": "You are shown ONE anchored unit from a real codebase, and a CLOSED LIST of the symbols THIS unit exports that are\nreferenced by OTHER units (resolved from the code index — every name below is a real, externally-called export of\nthis unit).\n\n<unit path=\"src/app.ts\" name=\"src/app.ts\">\nimport { greet } from './util';\nexport function main(): string { return greet('world'); }\n\n</unit>\n\nExternally-referenced exports (the closed list — select ONLY from these): \n\nFrom that list, pick the ONE export whose broad fan-in a competent engineer would NOT already expect from this\nunit's name and role — the symbol that is quietly a hub, whose \"referenced across the codebase\" status is worth\nrecording as knowledge.\n\n- The name you output MUST be copied VERBATIM from the candidate list above. Do not name anything else.\n- Do NOT state a number. Atlas computes the caller count from the index; your job is only to pick the salient name.\n- Restating an obvious hub (the export the unit plainly exists to provide) is not worth recording. Pick the\n  non-obvious one.\n- If the candidate list is empty, or none of its entries has a non-obvious fan-in worth recording, output the\n  single token `NO-FACT` and nothing else. That is a correct, expected abstention, never a failure.\n\nOutput EITHER exactly one line `COUNT: <name>` (with `<name>` copied verbatim from the list), OR the single token\n`NO-FACT`. No preamble, no reasoning, no number, no confidence, no formatting.\n"
        }
      ]
    },
    {
      "arm": "default",
      "exit": 0,
      "signal": null,
      "stdout": "genesis: seeded 0 candidate fact(s) [union]; ratified 0\ncost: llmCalls 6 · budgetSpent 6\nmine: arms — advisory 0 · dependency 0 · count 0\narm: advisory\nprompt: 441a39237b4e933bd685c122cb223214bf0a69627ecd98054f08e27e8d34ceb1 — the artifact every proposal on this run was built from\nmine: 0 candidate facts — 2 site(s) visited and every one abstained: nothing was proposed or admitted (facts are never fabricated)\ncoverage: coverage CLOSES — all 2 planned site(s) accounted for: 0 seeded, 2 abstained, 0 unrecorded, 0 interrupted, 0 never visited\nsite: {\"rank\":1,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/util.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\nsite: {\"rank\":2,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/app.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\narm: dependency\nprompt: 6015c264c18580c396befc5c2634feda6b1540d074e4ae0e46ad4acf6789ed67 — the artifact every proposal on this run was built from\nmine: 0 candidate facts — 2 site(s) visited and every one abstained: nothing was proposed or admitted (facts are never fabricated)\ncoverage: coverage CLOSES — all 2 planned site(s) accounted for: 0 seeded, 2 abstained, 0 unrecorded, 0 interrupted, 0 never visited\nsite: {\"rank\":1,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/app.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\nsite: {\"rank\":2,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/util.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\narm: count\nprompt: caa515d1a7c950b252546ee6e1286ea224731cc6688f942fda26db601c7f7bb3 — the artifact every proposal on this run was built from\nmine: 0 candidate facts — 2 site(s) visited and every one abstained: nothing was proposed or admitted (facts are never fabricated)\ncoverage: coverage CLOSES — all 2 planned site(s) accounted for: 0 seeded, 2 abstained, 0 unrecorded, 0 interrupted, 0 never visited\nsite: {\"rank\":1,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/util.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\nsite: {\"rank\":2,\"outcome\":\"abstained\",\"kind\":\"file\",\"path\":\"src/app.ts\",\"whyNot\":\"model abstained: no grounded fact at site\"}\n",
      "stderr": "",
      "prompts": [
        {
          "sha256": "0514c1445592f8475d4d2c87dd554225d0690eb6491a4901dba9e7ed1178397f",
          "dependency": false,
          "count": false,
          "block": true,
          "prompt": "You are shown ONE anchored unit from a real codebase, and nothing else.\n\n<unit path=\"src/util.ts\" name=\"src/util.ts\">\nexport function greet(n: string): string { return `hi ${n}`; }\n\n</unit>\n\nState ONE fact about this unit that a competent engineer would NOT already know from its name and\nsignature, and that would change what they do. Work in two steps.\n\nSTEP 1 — REASON FREELY (scratch, never stored). Read the bytes closely. Draft a candidate fact, then actively\ntry to REFUTE it against the source:\n- Is it derivable from THESE bytes, or are you leaning on what you know about a library, framework or\n  convention that is not visible here — or on a stale/past-tense COMMENT the current code may contradict?\n- Does the fact's truth rest on a COMMENT'S CLAIM about something NOT in these bytes — another file or\n  module, a task/PR/issue number (like #123), what the code \"used to\" do, or the absence of something\n  elsewhere in the codebase? If so, discard it: even an ACCURATE comment about elsewhere is a fact about\n  THAT code, not this unit, and does not re-derive at THIS unit's bytes. The surviving fact must be\n  checkable by reading the CODE in this unit itself — its declarations, control flow, types, or literals —\n  not by trusting what a comment asserts about code you cannot see here.\n- Does the claim assert an EXACT COUNT or an EXHAUSTIVE enumeration (\"N of the M items\", \"every X except\n  Y\", \"all fourteen …\")? A number or a complete list is admissible ONLY if you actually counted or listed\n  it in the shown bytes and re-checked it item by item — a miscount or a mislabelled item makes an\n  otherwise-true fact FALSE. Prefer the qualitative form (\"only X uses the type-only form, unlike its\n  siblings\") over a numeric one (\"N of the M re-exports …\") unless the exact number is itself the point and\n  you have verified it against the bytes.\n- Is it more than a restatement of the name, the signature or the types, or a summary of what the code\n  plainly does?\nDiscard any candidate that does not survive. This reasoning is scratch: it is parsed away and never persisted.\n\nSTEP 2 — EMIT THE RESULT. If a fact survived, emit it as EXACTLY ONE fenced block tagged `atlas-fact`, holding\na JSON object with a single `claim` field — one sentence of plain prose, derivable from the CODE bytes above\n(checkable by reading this unit itself, not from a comment's claim about code you cannot see here):\n\n```atlas-fact\n{\"claim\": \"<the one surviving fact>\"}\n```\n\nEmit exactly ONE such block, and put NO fenced block anywhere in your reasoning — only the final fact is fenced.\n\nIf no such fact survives, emit NO fact block at all (a bare `NO-FACT` on its own line is also accepted). Most\nunits hold none. That is a correct, expected result — recorded as a deliberate abstention, never a failure. A\nfact the bytes do not contain cannot be re-derived at source@sha and MUST NOT be emitted.\n"
        },
        {
          "sha256": "1e8eade32c4c06090a91bde1032553deb1ac95aa143cc9d9a1e4bece05ae60e0",
          "dependency": false,
          "count": false,
          "block": true,
          "prompt": "You are shown ONE anchored unit from a real codebase, and nothing else.\n\n<unit path=\"src/app.ts\" name=\"src/app.ts\">\nimport { greet } from './util';\nexport function main(): string { return greet('world'); }\n\n</unit>\n\nState ONE fact about this unit that a competent engineer would NOT already know from its name and\nsignature, and that would change what they do. Work in two steps.\n\nSTEP 1 — REASON FREELY (scratch, never stored). Read the bytes closely. Draft a candidate fact, then actively\ntry to REFUTE it against the source:\n- Is it derivable from THESE bytes, or are you leaning on what you know about a library, framework or\n  convention that is not visible here — or on a stale/past-tense COMMENT the current code may contradict?\n- Does the fact's truth rest on a COMMENT'S CLAIM about something NOT in these bytes — another file or\n  module, a task/PR/issue number (like #123), what the code \"used to\" do, or the absence of something\n  elsewhere in the codebase? If so, discard it: even an ACCURATE comment about elsewhere is a fact about\n  THAT code, not this unit, and does not re-derive at THIS unit's bytes. The surviving fact must be\n  checkable by reading the CODE in this unit itself — its declarations, control flow, types, or literals —\n  not by trusting what a comment asserts about code you cannot see here.\n- Does the claim assert an EXACT COUNT or an EXHAUSTIVE enumeration (\"N of the M items\", \"every X except\n  Y\", \"all fourteen …\")? A number or a complete list is admissible ONLY if you actually counted or listed\n  it in the shown bytes and re-checked it item by item — a miscount or a mislabelled item makes an\n  otherwise-true fact FALSE. Prefer the qualitative form (\"only X uses the type-only form, unlike its\n  siblings\") over a numeric one (\"N of the M re-exports …\") unless the exact number is itself the point and\n  you have verified it against the bytes.\n- Is it more than a restatement of the name, the signature or the types, or a summary of what the code\n  plainly does?\nDiscard any candidate that does not survive. This reasoning is scratch: it is parsed away and never persisted.\n\nSTEP 2 — EMIT THE RESULT. If a fact survived, emit it as EXACTLY ONE fenced block tagged `atlas-fact`, holding\na JSON object with a single `claim` field — one sentence of plain prose, derivable from the CODE bytes above\n(checkable by reading this unit itself, not from a comment's claim about code you cannot see here):\n\n```atlas-fact\n{\"claim\": \"<the one surviving fact>\"}\n```\n\nEmit exactly ONE such block, and put NO fenced block anywhere in your reasoning — only the final fact is fenced.\n\nIf no such fact survives, emit NO fact block at all (a bare `NO-FACT` on its own line is also accepted). Most\nunits hold none. That is a correct, expected result — recorded as a deliberate abstention, never a failure. A\nfact the bytes do not contain cannot be re-derived at source@sha and MUST NOT be emitted.\n"
        },
        {
          "sha256": "1e8eade32c4c06090a91bde1032553deb1ac95aa143cc9d9a1e4bece05ae60e0",
          "dependency": false,
          "count": false,
          "block": true,
          "prompt": "You are shown ONE anchored unit from a real codebase, and nothing else.\n\n<unit path=\"src/app.ts\" name=\"src/app.ts\">\nimport { greet } from './util';\nexport function main(): string { return greet('world'); }\n\n</unit>\n\nState ONE fact about this unit that a competent engineer would NOT already know from its name and\nsignature, and that would change what they do. Work in two steps.\n\nSTEP 1 — REASON FREELY (scratch, never stored). Read the bytes closely. Draft a candidate fact, then actively\ntry to REFUTE it against the source:\n- Is it derivable from THESE bytes, or are you leaning on what you know about a library, framework or\n  convention that is not visible here — or on a stale/past-tense COMMENT the current code may contradict?\n- Does the fact's truth rest on a COMMENT'S CLAIM about something NOT in these bytes — another file or\n  module, a task/PR/issue number (like #123), what the code \"used to\" do, or the absence of something\n  elsewhere in the codebase? If so, discard it: even an ACCURATE comment about elsewhere is a fact about\n  THAT code, not this unit, and does not re-derive at THIS unit's bytes. The surviving fact must be\n  checkable by reading the CODE in this unit itself — its declarations, control flow, types, or literals —\n  not by trusting what a comment asserts about code you cannot see here.\n- Does the claim assert an EXACT COUNT or an EXHAUSTIVE enumeration (\"N of the M items\", \"every X except\n  Y\", \"all fourteen …\")? A number or a complete list is admissible ONLY if you actually counted or listed\n  it in the shown bytes and re-checked it item by item — a miscount or a mislabelled item makes an\n  otherwise-true fact FALSE. Prefer the qualitative form (\"only X uses the type-only form, unlike its\n  siblings\") over a numeric one (\"N of the M re-exports …\") unless the exact number is itself the point and\n  you have verified it against the bytes.\n- Is it more than a restatement of the name, the signature or the types, or a summary of what the code\n  plainly does?\nDiscard any candidate that does not survive. This reasoning is scratch: it is parsed away and never persisted.\n\nSTEP 2 — EMIT THE RESULT. If a fact survived, emit it as EXACTLY ONE fenced block tagged `atlas-fact`, holding\na JSON object with a single `claim` field — one sentence of plain prose, derivable from the CODE bytes above\n(checkable by reading this unit itself, not from a comment's claim about code you cannot see here):\n\n```atlas-fact\n{\"claim\": \"<the one surviving fact>\"}\n```\n\nEmit exactly ONE such block, and put NO fenced block anywhere in your reasoning — only the final fact is fenced.\n\nIf no such fact survives, emit NO fact block at all (a bare `NO-FACT` on its own line is also accepted). Most\nunits hold none. That is a correct, expected result — recorded as a deliberate abstention, never a failure. A\nfact the bytes do not contain cannot be re-derived at source@sha and MUST NOT be emitted.\n"
        },
        {
          "sha256": "0514c1445592f8475d4d2c87dd554225d0690eb6491a4901dba9e7ed1178397f",
          "dependency": false,
          "count": false,
          "block": true,
          "prompt": "You are shown ONE anchored unit from a real codebase, and nothing else.\n\n<unit path=\"src/util.ts\" name=\"src/util.ts\">\nexport function greet(n: string): string { return `hi ${n}`; }\n\n</unit>\n\nState ONE fact about this unit that a competent engineer would NOT already know from its name and\nsignature, and that would change what they do. Work in two steps.\n\nSTEP 1 — REASON FREELY (scratch, never stored). Read the bytes closely. Draft a candidate fact, then actively\ntry to REFUTE it against the source:\n- Is it derivable from THESE bytes, or are you leaning on what you know about a library, framework or\n  convention that is not visible here — or on a stale/past-tense COMMENT the current code may contradict?\n- Does the fact's truth rest on a COMMENT'S CLAIM about something NOT in these bytes — another file or\n  module, a task/PR/issue number (like #123), what the code \"used to\" do, or the absence of something\n  elsewhere in the codebase? If so, discard it: even an ACCURATE comment about elsewhere is a fact about\n  THAT code, not this unit, and does not re-derive at THIS unit's bytes. The surviving fact must be\n  checkable by reading the CODE in this unit itself — its declarations, control flow, types, or literals —\n  not by trusting what a comment asserts about code you cannot see here.\n- Does the claim assert an EXACT COUNT or an EXHAUSTIVE enumeration (\"N of the M items\", \"every X except\n  Y\", \"all fourteen …\")? A number or a complete list is admissible ONLY if you actually counted or listed\n  it in the shown bytes and re-checked it item by item — a miscount or a mislabelled item makes an\n  otherwise-true fact FALSE. Prefer the qualitative form (\"only X uses the type-only form, unlike its\n  siblings\") over a numeric one (\"N of the M re-exports …\") unless the exact number is itself the point and\n  you have verified it against the bytes.\n- Is it more than a restatement of the name, the signature or the types, or a summary of what the code\n  plainly does?\nDiscard any candidate that does not survive. This reasoning is scratch: it is parsed away and never persisted.\n\nSTEP 2 — EMIT THE RESULT. If a fact survived, emit it as EXACTLY ONE fenced block tagged `atlas-fact`, holding\na JSON object with a single `claim` field — one sentence of plain prose, derivable from the CODE bytes above\n(checkable by reading this unit itself, not from a comment's claim about code you cannot see here):\n\n```atlas-fact\n{\"claim\": \"<the one surviving fact>\"}\n```\n\nEmit exactly ONE such block, and put NO fenced block anywhere in your reasoning — only the final fact is fenced.\n\nIf no such fact survives, emit NO fact block at all (a bare `NO-FACT` on its own line is also accepted). Most\nunits hold none. That is a correct, expected result — recorded as a deliberate abstention, never a failure. A\nfact the bytes do not contain cannot be re-derived at source@sha and MUST NOT be emitted.\n"
        },
        {
          "sha256": "0514c1445592f8475d4d2c87dd554225d0690eb6491a4901dba9e7ed1178397f",
          "dependency": false,
          "count": false,
          "block": true,
          "prompt": "You are shown ONE anchored unit from a real codebase, and nothing else.\n\n<unit path=\"src/util.ts\" name=\"src/util.ts\">\nexport function greet(n: string): string { return `hi ${n}`; }\n\n</unit>\n\nState ONE fact about this unit that a competent engineer would NOT already know from its name and\nsignature, and that would change what they do. Work in two steps.\n\nSTEP 1 — REASON FREELY (scratch, never stored). Read the bytes closely. Draft a candidate fact, then actively\ntry to REFUTE it against the source:\n- Is it derivable from THESE bytes, or are you leaning on what you know about a library, framework or\n  convention that is not visible here — or on a stale/past-tense COMMENT the current code may contradict?\n- Does the fact's truth rest on a COMMENT'S CLAIM about something NOT in these bytes — another file or\n  module, a task/PR/issue number (like #123), what the code \"used to\" do, or the absence of something\n  elsewhere in the codebase? If so, discard it: even an ACCURATE comment about elsewhere is a fact about\n  THAT code, not this unit, and does not re-derive at THIS unit's bytes. The surviving fact must be\n  checkable by reading the CODE in this unit itself — its declarations, control flow, types, or literals —\n  not by trusting what a comment asserts about code you cannot see here.\n- Does the claim assert an EXACT COUNT or an EXHAUSTIVE enumeration (\"N of the M items\", \"every X except\n  Y\", \"all fourteen …\")? A number or a complete list is admissible ONLY if you actually counted or listed\n  it in the shown bytes and re-checked it item by item — a miscount or a mislabelled item makes an\n  otherwise-true fact FALSE. Prefer the qualitative form (\"only X uses the type-only form, unlike its\n  siblings\") over a numeric one (\"N of the M re-exports …\") unless the exact number is itself the point and\n  you have verified it against the bytes.\n- Is it more than a restatement of the name, the signature or the types, or a summary of what the code\n  plainly does?\nDiscard any candidate that does not survive. This reasoning is scratch: it is parsed away and never persisted.\n\nSTEP 2 — EMIT THE RESULT. If a fact survived, emit it as EXACTLY ONE fenced block tagged `atlas-fact`, holding\na JSON object with a single `claim` field — one sentence of plain prose, derivable from the CODE bytes above\n(checkable by reading this unit itself, not from a comment's claim about code you cannot see here):\n\n```atlas-fact\n{\"claim\": \"<the one surviving fact>\"}\n```\n\nEmit exactly ONE such block, and put NO fenced block anywhere in your reasoning — only the final fact is fenced.\n\nIf no such fact survives, emit NO fact block at all (a bare `NO-FACT` on its own line is also accepted). Most\nunits hold none. That is a correct, expected result — recorded as a deliberate abstention, never a failure. A\nfact the bytes do not contain cannot be re-derived at source@sha and MUST NOT be emitted.\n"
        },
        {
          "sha256": "1e8eade32c4c06090a91bde1032553deb1ac95aa143cc9d9a1e4bece05ae60e0",
          "dependency": false,
          "count": false,
          "block": true,
          "prompt": "You are shown ONE anchored unit from a real codebase, and nothing else.\n\n<unit path=\"src/app.ts\" name=\"src/app.ts\">\nimport { greet } from './util';\nexport function main(): string { return greet('world'); }\n\n</unit>\n\nState ONE fact about this unit that a competent engineer would NOT already know from its name and\nsignature, and that would change what they do. Work in two steps.\n\nSTEP 1 — REASON FREELY (scratch, never stored). Read the bytes closely. Draft a candidate fact, then actively\ntry to REFUTE it against the source:\n- Is it derivable from THESE bytes, or are you leaning on what you know about a library, framework or\n  convention that is not visible here — or on a stale/past-tense COMMENT the current code may contradict?\n- Does the fact's truth rest on a COMMENT'S CLAIM about something NOT in these bytes — another file or\n  module, a task/PR/issue number (like #123), what the code \"used to\" do, or the absence of something\n  elsewhere in the codebase? If so, discard it: even an ACCURATE comment about elsewhere is a fact about\n  THAT code, not this unit, and does not re-derive at THIS unit's bytes. The surviving fact must be\n  checkable by reading the CODE in this unit itself — its declarations, control flow, types, or literals —\n  not by trusting what a comment asserts about code you cannot see here.\n- Does the claim assert an EXACT COUNT or an EXHAUSTIVE enumeration (\"N of the M items\", \"every X except\n  Y\", \"all fourteen …\")? A number or a complete list is admissible ONLY if you actually counted or listed\n  it in the shown bytes and re-checked it item by item — a miscount or a mislabelled item makes an\n  otherwise-true fact FALSE. Prefer the qualitative form (\"only X uses the type-only form, unlike its\n  siblings\") over a numeric one (\"N of the M re-exports …\") unless the exact number is itself the point and\n  you have verified it against the bytes.\n- Is it more than a restatement of the name, the signature or the types, or a summary of what the code\n  plainly does?\nDiscard any candidate that does not survive. This reasoning is scratch: it is parsed away and never persisted.\n\nSTEP 2 — EMIT THE RESULT. If a fact survived, emit it as EXACTLY ONE fenced block tagged `atlas-fact`, holding\na JSON object with a single `claim` field — one sentence of plain prose, derivable from the CODE bytes above\n(checkable by reading this unit itself, not from a comment's claim about code you cannot see here):\n\n```atlas-fact\n{\"claim\": \"<the one surviving fact>\"}\n```\n\nEmit exactly ONE such block, and put NO fenced block anywhere in your reasoning — only the final fact is fenced.\n\nIf no such fact survives, emit NO fact block at all (a bare `NO-FACT` on its own line is also accepted). Most\nunits hold none. That is a correct, expected result — recorded as a deliberate abstention, never a failure. A\nfact the bytes do not contain cannot be re-derived at source@sha and MUST NOT be emitted.\n"
        }
      ]
    }
  ],
  "defaultAllAdvisory": true,
  "controls": [
    {
      "arm": "advisory",
      "n": 2,
      "exit": 0
    },
    {
      "arm": "dependency",
      "n": 2,
      "exit": 0
    },
    {
      "arm": "count",
      "n": 2,
      "exit": 0
    }
  ]
}
